# 避難所検索機能 移植用プロンプト

以下をそのまま AI に投げてください。

---

あなたはWebアプリ開発の実装担当です。今の要件は、避難所検索結果画面の一部を既存の別システムに移植することです。既存アプリの構造に合わせて実装してください。

実装対象は以下です。

1. 避難所検索結果一覧画面
2. 地区名検索
3. 条件絞り込み（ペット可 / 車椅子対応 / 多目的トイレ）
4. 現在地取得して距離を計算し、近い順に並び替える
5. ルート案内リンク（Google Maps の徒歩ルート）
6. 連絡先欄の表示
7. 住所の次に連絡先を表示するレイアウト

## 実装要件

- 避難所一覧には以下の列を持つテーブルを作成する
  - 避難所名
  - 住所
  - 連絡先
  - 混雑状況
  - ペット可否
  - 車椅子対応
  - 多目的トイレ
  - 現在地からの距離
  - ルート案内
- 「周囲の目印となる建物からの距離、時間」の欄は不要なので削除する
- 住所の直後に「連絡先」欄を追加する
- 連絡先は避難所データの `phone` または `contact` フィールドから表示する
- 連絡先が未入力なら「—」と表示する
- 検索画面で以下のフィルターを作成する
  - ペット可
  - 車椅子対応
  - 多目的トイレ
- フィルターは複数選択可能
- フィルターを選択して検索した場合、条件に合う避難所だけを表示する
- 地区名検索とフィルター条件を併用できる
- 検索結果画面でもフィルターを再設定できるようにする
- フィルター状態を維持したまま結果を表示する
- 「現在地を取得してルート案内」ボタンを設置する
- `navigator.geolocation` を使って現在地を取得する
- 各避難所の緯度・経度がある場合、現在地との距離を計算する
- 距離の計算には Haversine の式を使う
- 計算結果は km 単位で小数第1位まで表示する
- 距離が近い順に並び替える
- 距離が判定できない避難所は「—」を表示する
- ルート案内は Google Maps の徒歩ルートリンクを生成する
  - URL は `https://www.google.com/maps/dir/` を使う
  - `origin` を現在地、`destination` を避難所の住所もしくは施設名に設定する
  - `travelmode=walking` を指定する
- ルート案内リンクは「徒歩ルートを見る」に変える
- 位置情報の許可がない場合は、適切なメッセージを表示する
- 位置情報取得中はボタンの文言を変更し、状態を明示する

## データ設計

- 避難所のデータ構造には以下を許容する
  - `name`
  - `address`
  - `phone`
  - `district`
  - `latitude`
  - `longitude`
  - `pets`
  - `wheelchair`
  - `toilet`
  - `congestion`
- フィルター判定は、値が `可` / `対応` / `あり` / `可能` / `有` などの肯定表現を含む場合に対象とし、`不可` / `なし` / `不可能` / `未対応` は除外する
- `district` で検索する場合、完全一致または同一文字列で判定する

## UI/UX要件

- 既存デザインに合わせて、軽いカード風またはテーブル風の検索結果画面を作る
- フィルターはボタン風またはチェックボックス風で見やすくする
- 近い順がすぐ分かるように、距離欄を大きく見せる
- 画面が狭い場合でも横スクロールに耐えるようにし、見切れないようにする
- 一覧の見た目はシンプルで、表を使う場合はヘッダーとボーダーを明確にする

## 実装方針

- 既存アプリのフレームワークに合わせて実装する
- サーバー側で検索条件の絞り込みを行う
- フロント側で現在地距離計算と並び替えを行う
- ルーティング、テンプレート、JS を適切に分離して実装する
- 機能が動くことを確認するために、最低限のテストまたは動作確認を行う

## 完成条件

以下が満たされていれば完成とする。

- 避難所検索結果一覧が表示される
- 地区名検索が動く
- フィルターによる絞り込みが動く
- 連絡先が住所の次に見える
- 現在地を取得して距離が表示される
- 距離が近い順に並び替わる
- Google Maps の徒歩ルート案内リンクが機能する
- 近い順・遠い順のロジックがユーザーの意図どおりに動く
- 適切な空状態や例外メッセージが表示される

## 返答形式

実装後は、以下を含めて返してください。

- 変更したファイル一覧
- 実装内容の簡潔な要約
- 主要な修正ポイント
- どこで検索・絞り込み・距離計算・ルート案内が動いているか
- 確認方法

以上が今回の要件です。既存システムに自然に組み込めるように、最小修正で実装してください。

---

この指示をそのまま実行してください。
